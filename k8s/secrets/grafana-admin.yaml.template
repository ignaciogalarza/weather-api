apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  labels:
    app: grafana
type: Opaque
stringData:
  # Replace these values with your own credentials
  username: admin
  password: ${GRAFANA_ADMIN_PASSWORD}
